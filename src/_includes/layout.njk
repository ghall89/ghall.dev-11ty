<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ghall.dev - {{ title }}</title>
    <link rel="stylesheet" href="/style/tailwind.css" />
    <script src="/js/alpine.js"></script>
  </head>
  <body>
    <main
      class="flex min-h-screen flex-col bg-gradient-to-br from-indigo-900 to-red-900 px-10 text-white"
    >
      <nav class="max-w-screen-md:px-5 mb-4 py-6">
        <div class="mx-auto flex max-w-screen-lg items-center justify-between">
          <h1 class="text-3xl font-bold text-white">ghall.dev</h1>
          <!-- <ul class="flex gap-10">
            {% for link in nav %}
            <li>
              <a
                class="flex gap-2 text-lg font-thin uppercase text-sky-100 transition-colors hover:text-white"
                href="{{ link.href }}"
                ><embed
                  class="text-white"
                  src="/icons/{{ link.icon }}.svg"
                /><span>{{ link.label }}</span></a
              >
            </li>
            {% endfor %}
          </ul> -->
        </div>
      </nav>
      <div
        class="max-w-screen-md:px-5 mx-auto flex max-w-screen-lg flex-grow items-center"
      >
        {{ content | safe }}
      </div>
      <footer class="mt-4 flex justify-center py-8">
        <span>2024 - Graham Hall</span>
      </footer>
    </main>
  </body>
</html>
