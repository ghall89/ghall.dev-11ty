---
layout: layout.njk
title: Projects
---

<ul class="mt-3">
  {% for project in projects %}
  <li>
    <h3 class="text-2xl">{{ project.name }}</h3>
    <p>{{ project.description }}</p>
    <a
      href="{{ project.url }}"
      target="_blank"
      class="text-sky-600 transition-all hover:text-sky-500 hover:underline"
      >Check it out!</a
    >
  </li>
  {% endfor %}
</ul>
